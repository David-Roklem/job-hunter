/**
 * Drizzle-схема.
 *
 * В фазе bootstrap — минимальная заглушка, чтобы `db:generate` отрабатывал
 * и создавал первую миграцию. Реальные таблицы (вакансии, резюме, отклики,
 * источники, логи) добавляются в фазе 2.
 *
 * Экспортируем пустой объект-агрегат схемы, чтобы drizzle-kit нашёл файл,
 * но не генерировал пустые таблицы.
 */
export {};
