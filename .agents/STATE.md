---
milestone: 0.1.0
current_position: Фаза 10 (review-ui) ВЫПОЛНЕНА на ветке `review-ui` (ожидает merge). UI инбокс `/applications`: показывает applications с cover_letter, действия одобрить (status→approved) / отклонить (→rejected) / регенерировать (generateCoverLetter) / редактировать письмо (отдельная страница /applications/:id/edit, coverLettersRepo.updateBody → edited_at). Главная: плашки «Отклики»/«Резюме» кликабельны. Автотесты 219/219 (+14 новых), tsc чистый. Ручной smoke валиден (approve/reject/regenerate/save/404 на реальном dev-сервере). ТОЧКА ПРОДОЛЖЕНИЯ: merge review-ui → master, затем ROADMAP фаза 11 (apply-hh — авто-отклик через Playwright) или 12 (scheduler — фоновый цикл сбора+скоринга+генерации). Фаза 09 влита в master.
camoufox-stealth ВЫПОЛНЕНА + влита в master (merge 78578a3).
last_updated: 2026-07-15
---

# Project state — job_hunter

Use `/soly` to see current state. Use `/plan N` to plan phase N.

## Decisions

| Date | Decision | Why |
| Стек: TypeScript + Remix (route-per-file, loader/action, cookie-сессии) — соответствует правилам проекта (routing.md, auth.md) | Единый язык full-stack; правила проекта (.agents/rules) явно описывают Remix-подобную модель маршрутов и cookie-сессий | — |
| Запуск: локально, single-user — без хостинга/биллинга/регистрации | Простейший путь для личного использования, минимум инфраструктуры | — |
| Источники вакансий: hh.ru (Playwright), сайты компаний/aggregator'ы (парсинг), Telegram-каналы (Telegram API) | От API hh.ru отказались из-за бюрократии OAuth — используем браузерную автоматизацию; Telegram и aggregators добавляют покрытие | — |
| Уровень автоматизации: «готовит, вы подтверждаете» — система ищет/фильтрует/пишет черновик отклика+письма, одобрение одним кликом | Безопаснее и выше конверсия, чем полный автопилот; страхует от кривых откликов | — |
| AI-провайдер: Yandex GPT (и/или GigaChat) — оплата из РФ, знает русскоязычный рынок труда | Российский рынок, оплата картой РФ, хорошее знание контекста HH/российских вакансий | — |
| Резюме: несколько шаблонов под разные роли; матчинг/адаптация под вакансию из выбранного шаблона | Пользователь ищет работу по нескольким направлениям — нужны разные версии резюме | — |
| Нагрузка: средняя (~100 откликов/день) — нужен фоновый планировщик + очередь задач, анти-лимиты | При таком объёме синхронная обработка не подходит; Playwright-сессии hh.ru чувствительны к частоте запросов | — |
| Фреймворк: React Router v7 (framework mode) вместо устаревшего create-remix | create-remix официально deprecated и мигрирует в RR7; план буквально ссылается на deprecated инструмент. RR7 сохраняет route-per-file, loader/action, Route.LoaderArgs из routing.md. | — |
| SQLite-драйвер: встроенный node:sqlite (Node 24) вместо better-sqlite3 ~~ОТМЕНЕНО~~ — см. ниже | ~~better-sqlite3 требует нативной сборки node-gyp~~ — оказалось неверным: в drizzle-orm 0.45.2 драйвера node-sqlite не существует. ОТМЕНЕНО фазой 02. | — |
| Корневая директория исходников: app/ (стандарт Remix/RR7) вместо src/ из routing.md | Совпадает с шаблоном фреймворка, меньше отклонений, проще обновляться. routing.md носит иллюстративный характер — семантика (route-per-file, co-located loader/action, Route.LoaderArgs) сохраняется. | — |
| SQLite-драйвер: better-sqlite3 (вместо node:sqlite из bootstrap). В drizzle-orm 0.45.2 драйвера node-sqlite не существует — bootstrap оставил несуществующий импорт. | better-sqlite3 — единственный Node.js SQLite-драйвер Drizzle, поддерживающий и рантайм, и drizzle-kit migrate в этой версии. Отменяет решение bootstrap-фазы. | — |
| Реляционный query API Drizzle (db.query.*.findFirst/findMany) асинхронный даже для sync-драйвера better-sqlite3 — репозитории используют async для findById/list, sync для прямых select/insert/update. | findFirst/findMany возвращают thenable (Promise). Прямые db.select/insert/update дают .get()/.all() — sync. Смешанная модель соответствует API Drizzle. | — |
| Фаза 03 = репозиторий resume_templates + UI (маршруты RR7: список/создание/редактирование/удаление) + загрузка markdown и PDF-текста + CRUD-тесты на in-memory SQLite. | Фаза 02 закрыла репозитории без UI; фаза 03 даёт пользователю первый полезный экран. Строгая zod-схема опыта подготовлена под matcher (08) и draft-generator (09). | 3 |
| Тестируемость БД-слоя: createDb() фабрика + db-синглтон в app/db/index.ts. Репозиторий resume_templates — функции от db-параметра (create/findById/list/update/remove принимают db первым аргументом) + именованные обёртки без аргумента для feature-кода (берут синглтон). vi.mock("~/db") отвергнут — алиас ~ не настроен в vitest.config.ts, а путь с кириллицей ломает vite-трансформ. | vi.mock на ~/пути ненадёжен в этом окружении (нет алиаса в vitest.config, кириллица в cwd ломает transform). DI-параметр чистый, предсказуемый, не требует vi.mock и не ломает feature-код (обёртки делегируют синглтону). | 3 |
| Тестируемость БД-слоя: createDb() фабрика + db-синглтон в app/db/index.ts; алиас "~" добавлен в vitest.config.ts (был только в vite.config.ts); тесты используют vi.mock("~/db", () => ({ db })) с in-memory better-sqlite3 + migrator. Репозиторий resume_templates — единый стиль с sources.ts (модульный импорт db), БЕЗ DI-параметров. DI-вариант отвергнут: нарушает единообразие с тремя существующими репозиториями. | vi.mock("~/db") работает чисто, если алиас ~ настроен в vitest.config (раньше был только в vite.config). Это сохраняет единый стиль всех репозиториев (модульный db) и не требует прокидывать db аргументом. | 3 |
| AI-провайдер: z.ai (Zhipu AI, GLM-5.1) вместо Yandex GPT. У пользователя PRO-подписка z.ai. | Пользователь явно указал PRO-подписку z.ai и отказался от Yandex GPT. z.ai проще в интеграции: аутентификация — прямой API-ключ Bearer (без IAM-обмена как у Yandex), OpenAI-совместимый chat completions endpoint. Отменяет решение из STATE.md «AI-провайдер: Yandex GPT». | 4 |
| Аутентификация z.ai: прямой API-ключ через Authorization: Bearer, без IAM-токенов/refresh-логики. | z.ai API использует простую Bearer-схему с долгоживущим API-ключом (docs.z.ai, актуально 2026-06). IAM-обмен с кэшированием (как у Yandex) не нужен — меньше кода, меньше точек отказа. Ключ из env (ZAI_API_KEY). | 4 |
| Объём фазы 04: провайдер-слой (интерфейс AiProvider + реализация z.ai) + промпт-шаблоны + generateCoverLetter(applicationId) с записью в cover_letters.body. Адаптация резюме и UI — фаза 09. | Пользователь выбрал «полный цикл до записи в БД», но запись — в cover_letters (письмо). Это даёт первый end-to-end срез AI-функциональности (ввод→LLM→БД), который фаза 09 (draft-generator) расширит UI-оркестрацией и адаптацией резюме. | 4 |
| Модель z.ai берётся из env (ZAI_MODEL, дефолт "glm-5.1"), не хардкодится. Язык промптов — параметризуется локалью/источником (русский по умолчанию, заложена мультиязычность под будущие зарубежные площадки). | Пользователь: модель через env (PRO-подписка может быть привязана к конкретной модели); язык промптов зависит от площадки, в перспективе зарубежные рынки. Локаль промптов — параметр, а не хардкод. | 4 |
| Фаза 05 (source-hh): сбор вакансий hh.ru через Playwright + бинарный include/exclude фильтр при сборе → vacancy.status. БЕЗ авто-отклика (фаза 11) и БЕЗ полного скоринга (matcher 08). | Пользователь хочет фильтр «подходит/не подходит» уже при сборе (по title+тегам+описанию), чтобы не тащить мусор в БД. Бинарный include/exclude — предсказуем, без AI, не дублирует полноценный matcher (08) со скорингом. Apply-hh вынесен в фазу 11 по ROADMAP. | 5 |
| Сессия hh.ru: Playwright storageState в файл (data/hh-session.json). Первый запуск — ручной логин в видимом браузере (headed), куки+localStorage сохраняются; далее headless с переиспользованием storageState. | Стандартный паттерн Playwright для сайтов с логином. Ручной логин (с капчей/2FA) один раз → куки персистятся → фоновый планировщик (фаза 12) работает headless без человека. Автологин по паролю отвергнут: hh почти всегда требует капчу/2FA. | 5 |
| Критерии поиска: новая таблица search_profiles (несколько профилей под разные роли/направления). Поля: name, query, areas, employment_types, include_keywords[], exclude_keywords[], min_salary. Связана с source.config (какой профиль applied к какому source). Парсинг: список вакансий + детальные страницы (для тегов/ключевых навыков, нужных фильтру). | Vision: multiple resume templates под разные роли → логично несколько профилей критериев. Include/exclude фильтру нужны теги/навыки из детальных страниц (в списке их нет). Одного source.config недостаточно — критерии персистируются и редактируются (UI позже). | 5 |
| Запуск сбора: standalone CLI-скрипт scripts/collect-hh.ts (не RR action). Playwright-сессия долгая (~минуты) — не для HTTP-запроса. Тесты: vi.mock playwright (парсеры + маппинг hh→БД + include/exclude), ручной smoke с реальным hh (нужен логин). | Сбор — минуты, HTTP-таймауты. CLI вызывается вручную или планировщиком (фаза 12). Реальный hh в автотестах = бан-риск + нужен логин → мок Playwright на парсеры, ручной smoke отдельно (как smoke-zai в фазе 04). | 5 |
| Анти-детект уровень 2: ручные init-scripts (addInitScript) + поведенческая имитация + persisted context. БЕЗ устаревших playwright-extra/stealth-пакетов (последнее обновление 2023, несовместимы с Playwright 1.61). БЕЗ анти-детект-браузеров (Camoufox и т.п. — избыточно для single-user hh). | playwright-extra (v4.3.6, 2023-03) и puppeteer-extra-plugin-stealth (2023-04) устарели и могут ломаться на Playwright 1.61. Ядро stealth = набор init-script патчей (navigator.webdriver=false, window.chrome, navigator props, webgl, plugins) + human-like поведение (задержки, скролл, мышь) + реальный persisted-профиль вместо newContext. Реализуется вручную ~100 строк, полностью контролируемо, тестируемо. Анти-детект-браузеры — оверкилл для локального single-user. | 5 |
| WebGL vendor/renderer НЕ маскируется: реальный GPU-отпечаток (напр. "Google Inc. (NVIDIA)" через ANGLE на десктопе пользователя) — правдоподобный десктопный отпечаток, а не headless-маркер. Подмена на Intel создавала бы несоответствие и ухудшала скрытность. Маска включается только если окружение реально headless (SwiftShader). | stealth-check показал "Google Inc. (NVIDIA)" — это настоящее железо пользователя через ANGLE, выглядит как легитимный десктоп. Headless-маркер — это "SwiftShader"/"Google Inc. (Google)". Подмена реального отпечатка на чужой (Intel) — антипаттерн: детекторы ловят несоответствия между UA/GPU/экраном. | 5 |
| Фокус проекта смещён с «только РФ» на «международный/зарубежный рынок + hh.ru». Добавлен Wellfound как первый зарубежный источник (фаза 06). | Пользователь явно указал смену фокуса на зарубежный рынок. hh.ru остаётся. Wellfound — зарубежная площадка с сильной JS-разметкой (SPA), что делает его хорошим референсом для Playwright-парсинга. Vision.md обновлён, non-goal «поддержка зарубежных рынков» убран. | 6 |
| Фаза 08 matcher: двухуровневый алгоритм — rule-based префильтр (пересечение навыков resume в тексте вакансии) → AI-скоринг z.ai только для прошедших. Score 0–100 int, AI возвращает {score, rationale}. Результат: applications (status='matched', match_score) + vacancy.status='matched'. Без UI. | Пользователь выбрал AI-скоринг, но 73 вакансии × N шаблонов = дорого; rule-префильтр отсекает мусор до AI-запроса (соответствует бинарному фильтру фазы 05). applications.match_score уже в схеме — переиспользуем, не плодим таблицу matchings. | 8 |
| matchAll берёт вакансии status IN ('new','matched') (не только 'new'). Сборщики фаз 05–07 выставляют 'matched' сразу при прохождении source-фильтра, а 'new' практически не остаётся — поэтому батч --all иначе никогда не подхватывал уже собранные вакансии. 'rejected' исключён осознанно. | Smoke-прогон выявил: matchAll по 'new' давал 0 вакансий (все 155 уже matched после фазы 07). 'matched' после source-фильтра = валидный кандидат для AI-скора, семантически корректно включить его в пул matcher'а. | 8 |
| Фаза 09 = только сопроводительное письмо (резюме = загруженный пользователем шаблон как есть, БЕЗ AI-адаптации). generateDrafts переиспользует generateCoverLetter (фаза 04), добавляя батч-оркестрацию (generateDraftsAll: continue-on-error + дедуп по cover_letter + minScore) и точки запуска (CLI npm run generate-drafts + RR action /drafts + smoke). | Пользователь загружает резюме вручную и хочет полный контроль над ним. AI-адаптация резюме (переупорядочивание/перестановка акцентов) отложена как опция — после review-ui (фаза 10), когда реальная потребность проявится. generateCoverLetter уже существовал (фаза 04), но нигде не вызывался — фаза 09 доводит его до продакшена. | 9 |
| Фаза 10 review-ui: инбокс `/applications` показывает applications с cover_letter (любой статус). Действия: одобрить (status→approved) / отклонить (→rejected) / регенерировать (generateCoverLetter фазы 04) / редактировать письмо (отдельная страница /applications/:id/edit → coverLettersRepo.updateBody ставит edited_at). Главная: плашки «Отклики»/«Резюме» кликабельны. Без ручного dropdown'а статусов — предсказуемость. | Письмо = готовность к ревью (соответствует vision «система готовит → ты одобряешь»). updateBody (а не upsert) отличает ручную правку от AI-генерации по edited_at — важно для аудита. Паттерны UI повторяют фазу 03 resumes (loader/action, Route.LoaderArgs, throw data для 404, intent-ветвление). | 10 |
| fingerprint-pinning: зафиксирован BrowserForge Fingerprint (1 раз → data/hh-fingerprint.json) и пробрасывается в launch_server(fingerprint=...) при каждом запуске через serve.py --fingerprint. hh видит один отпечаток между login и collect/apply → не инвалидирует сессию. | Camoufox 0.4.11 генерирует случайный fingerprint (BrowserForge) при каждом launch_server(); storageState хранил только куки без UA/screen → hh видел валидную hhtoken, но отпечаток не совпадал с тем, при котором кука выдана → silent разлогин. Round-trip Fingerprint↔JSON (через dataclasses.asdict + рекурсивный from_dict по вложенным ScreenFingerprint/NavigatorFingerprint/VideoCard) доказан. Остаёмся на 0.4.11 + playwright 1.50 (проверенный стек); fingerprint_preset (бандл пресетов) отложен — требует 0.5.3 → playwright 1.60, риск WS-handshake fail. Доп.находка: маркеры isLoggedIn устарели (mainmenu_myResumes → mainmenu_profileAndResumes) — тот же класс бага что vacancyCard. | — |
| apply-hh: авто-отклик submitApplication(applicationId) — открывает форму /applicant/vacancy_response?vacancyId=X напрямую, подставляет cover_letters.body_md, submit. status → sent/failed. Маппинг резюме через таблицу hh_resume_mapping (resume_template_id → hh resume_id), заполняется скриптом hh:map-resumes. | Каноничный URL формы надёжнее клика по кнопке (не зависит от JS-модалки). hh не принимает resumeId в URL и не даёт dropdown data-qa → маппинг 1:1 в таблице — простейший рабочий путь. Submit идёт с текущим активным резюме (авто-выбор по имени не доработан — hh рендерит dropdown JS непредсказуемо); достаточно пока активно одно целевое резюме. | — |
|------|----------|-----|
| 2026-07-13 | Фаза 06 = Wellfound (aggregator) через Playwright поверх общего browser/session. Расширен sourceKinds += 'aggregator'. Вынесен app/browser/session.ts (locale/timezone/profileDir-параметризованный). Wellfound SPA → waitForSelector. Свой parseSalary под $/k. | Второй источник после hh; первое переиспользование инфраструктуры фазы 05. План: app/wellfound/ зеркало app/hh/. | 6 |
| 2026-07-13 | Ручной smoke Wellfound ОТЛОЖЕН: Cloudflare bot-detect блокирует Playwright по IP (202.148.55.56) ещё до формы логина. Уровень анти-детекта 2 (ручные stealth) недостаточен для Wellfound. | Блок внешний, не баг кода (автотесты 80/80 зелёные). Smoke hh (фаза 05) тоже pending. Лечится эскалацией анти-детекта. | 6 |
| 2026-07-13 | Эскалация анти-детекта: Camoufox (Firefox-based) как ОБЩИЙ браузер-стек для всех источников (hh + wellfound). | Пользователь выбрал Camoufox после bot-detect'а Wellfound. Заменит ядро app/browser/session.ts (Chromium → Camoufox). Отдельный план/фаза; повторный smoke обоих источников после миграции. | 6 |
| 2026-07-13 | Camoufox через Python-bridge: Python (camoufox@0.4.11) launch_server → Node firefox.connect. ОБЩИЙ стек для всех источников. JS-порт camoufox@0.1.19 отвергнут (3 бага). | Cloudflare заблокировал Playwright/Chromium на Wellfound (фаза 06). Camoufox (модифицированный Firefox, FingerprintForge) обходит его. Python-порт стабилен, JS-порт сырой. | camoufox-stealth |
| 2026-07-13 | playwright pinned 1.50.0 (JS + Python) — протокольный матч для firefox.connect. | Эмпирическая находка POC: 1.61 даёт WS-handshake fail (version skew с Camoufox-driver). Зафиксировано в обоих манифестах. | camoufox-stealth |
| 2026-07-13 | python-bridge/package.json БЕЗ type:module — иначе camoufox's launchServer.js (CJS) заражается корневым ESM-флагом. | Эмпирическая находка: launch_server валился с require-is-not-defined из-за апстрим package.json type:module. | camoufox-stealth |
| 2026-07-13 | Селекторы Wellfound (data-testid) НЕ совпадают с реальностью — переносится в отдельную работу. camoufox-stealth закрыт на главной цели (Cloudflare пройден). | Smoke нашёл: data-testid нет, карточки на Tailwind + a[href*=/jobs/]. Пользователь решил не раздувать stealth-фазу фиксом парсеров. | camoufox-stealth |
| 2026-07-14 | Дампер Wellfound (scripts/dump-wellfound-html.ts, npm run wellfound:dump) — Camoufox-съём отрендеренного HTML (search + N детальных) в data/dumps/. Готов, ждёт возможности пройти блок. | Нужен реальный HTML для правки selectors.ts/parsers.ts (синтетические фикстуры — гадание). | wellfound-selectors-fix |
| 2026-07-14 | КОРРЕКЦИЯ camoufox-stealth: Wellfound блокирует по IP (DataDome + «Access restricted IP 202.148.55.56»), не по fingerprint. Camoufox НЕ обходит это — он меняет fingerprint, но не IP. Утверждение SUMMARY «Cloudflare пройден, collect работает» верно только для headed+залогиненного, и то временно. | Дамп headless (2026-07-14) показал DataDome interstitial; headed повтор дал тот же IP-блок. Блок персистентен по IP с фазы 06. Лечится ТОЛЬКО прокси (residential/mobile) + geoip sync в Camoufox. | wellfound-selectors-fix |
| 2026-07-14 | Прокси для Wellfound ОТЛОЖЕН пользователем. Запланированное: WF_PROXY env → serve.py launch_server(proxy, geoip=True), только Wellfound (hh на прямом соединении). Camoufox API подтверждён context7 (daijro/camoufox, 2026-06): launch_server принимает proxy в Playwright-формате. | Пользователь передумал после выбора «прокси в стек» — не готов к выбору/оплате прокси-сервиса. Wellfound заморожен до решения по сети. Дампер и ветка сохранены. | wellfound-selectors-fix |
| 2026-07-14 | Фаза 07 = Telegram-источник через MTProto/gramjs (user-аккаунт), НЕ Bot API. Чтение публичличных каналов вакансий + полная история. | Bot API не может читать чужие публичличные каналы (только где бот админ) — большинство вакансий-каналов чужие. gramjs под user-аккаунтом читает любой публичличный канал. api_id/api_hash бесплатно на my.telegram.org. Подтверждено context7 (gram-js/gramjs, v2.26.22): TelegramClient + StringSession + getMessages(entity, {minId}). | 7 |
| 2026-07-14 | Гибридный парсинг постов Telegram: регэкспы для скелета (title/url/contacts/location) + z.ai для зарплаты из свободного текста. Прелиминар-регэксп отсекает посты без признаков зарплаты (нет AI-вызова). | Посты неструктурированны; регэкспы дёшевы для скелета, AI точнее для «вилка 250–350к»/«$120k». Прелиминар экономит ~AI-запросы (~100 постов/день). z.ai уже есть (фаза 04). | 7 |
| 2026-07-14 | Каналы в ОТДЕЛЬНОЙ таблице telegram_channels (id, source_id FK, username unique, title, last_message_id курсор, is_active). Один source(kind=telegram) → много каналов. | Курсор последнего поста — per-channel состояние, не помещается в sources.config_json чисто. Один source собирает из 5–10 каналов вакансий под один профиль критериев. | 7 |
| 2026-07-14 | external_id = message_id канала (строка), url = https://t.me/<channel>/<message_id>. Подходит под существующий UNIQUE(source_id, external_id). | message_id уникален в канале; url t.me каноничен и кликабелен. Переиспользуем дедупликацию и схему vacancies без изменений. | 7 |
| 2026-07-14 | env: TELEGRAM_BOT_TOKEN (от bootstrap) ЗАМЕНЕН на MTProto-ключи TG_API_ID/TG_API_HASH/TG_SESSION (опциональны для приложения, обязательны для telegram-сборa). | MTProto, не Bot API — bot token не нужен. Опциональность: telegram-функциональность изолирована, приложение/тесты работают без telegram-ключей. | 7 |
| 2026-07-14 | Дедупликация JS regex с кириллицей: \b (ASCII-only) НЕ работает для русских слов — заменён на lookbehind (?<![\p{L}]) / lookahead на не-букву с флагом u. Падежи: паттерн = корень слова (москв → Москва/Москвы/Москве). | Найдено тестами: parseLocation возвращал null для «в Москве» (\b не матчит кириллицу + падежные окончания не входили в [аы]). Корень слова покрывает все падежи. | 7 |
| 2026-07-08 | Initial scaffold | Created by `soly init` |
