# Dependencies
node_modules

# Build output
/build
/.cache

# React Router generated types
/.react-router

# Environment & secrets
.env
.env.*
!.env.example

# SQLite database & journals
/data/*.sqlite
/data/*.sqlite-journal
/data/*.sqlite-wal
/data/*.sqlite-shm
*.sqlite
*.sqlite-journal

# hh.ru session/profile (куки, localStorage, cache) — содержит приватную сессию
/data/hh-profile/
/data/hh-session.json

# Wellfound session/profile (куки, localStorage, cache) — содержит приватную сессию
/data/wellfound-profile/

# HTML-дампы сайтов (реальный отрендеренный HTML с приватными данными сессии)
/data/dumps/

# Logs
*.log
npm-debug.log*

# Soly iteration checkpoints & rule-mtime cache (local)
.agents/iterations/
.agents/rule-mtimes.json

# pi local todos (session-local)
.pi/todos/

# OS
.DS_Store
Thumbs.db

# Python-bridge (uv environment)
/python-bridge/.venv/
